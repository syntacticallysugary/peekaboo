fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
httpx>=0.27.0
# insightface 0.7.3 pure-Python runtime deps
# onnxruntime and opencv are intentionally excluded — l4t-ml provides
# NVIDIA-optimized builds; insightface is installed --no-deps to avoid
# those being overwritten.
tqdm
Pillow
scipy
prettytable
scikit-image
albumentations
matplotlib
requests
